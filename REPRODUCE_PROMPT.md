# 复现 Prompt · 中国古诗词展示页面

> 目标：让任意 AI Agent 复现出 `youngtongyang.github.io/Chinese-Poem` 的效果。
>
> ⚠️ **重要前提**：这个页面**不是单一 prompt 一次生成的**，而是三段式工作流：
>
> ```
> ① 出图      AI 生成国风水墨插画（2 张主图 + 1 张人物像）
> ② 排版      手写 HTML/CSS 单文件页面
> ③ 交互      内联 JS 物理引擎（春风拂柳 + 拖拽回弹）
> ```
>
> 如果你的对比对象只能"一个 prompt 出整个页面"，那结果必然不同 —— 那不是模型差距，是工作流差距。**建议让对方按同样的三段式来，才公平。**
>
> 最简可复现路径：把第 ②③ 段的 prompt 丢给 agent（插画可用占位图替换），20 分钟内能出一个能跑的版本。

---

## 一、出图 Prompt（第 ① 段）

模型：`Doubao-Seedream-5.0-lite`，分辨率 `2048x2048`，每张出 1 版。
（备选：任何中文语义理解好的文生图模型，如 Seedream 4.0 / 即梦 / 通义万相）

### 共用画风约束（STYLE）

> 中国新国潮水墨插画风格，融合传统水墨晕染与现代商业插画精致感。整体色调低饱和度，仿古宣纸米黄色底，暖赭石色与淡墨青为主。画面叠加明显的古旧宣纸颗粒纹理质感，古朴典雅。写意水墨笔法，留白为主，意境悠远。中国古典绘画审美，无任何文字、无印章、无人物。

### Prompt A：主插画（诗意场景）

> 李白《望庐山瀑布》诗意场景。「日照香炉生紫烟，遥看瀑布挂前川。飞流直下三千尺，疑是银河落九天。」画面主体：庐山香炉峰高耸入云，山体巍峨险峻，一道巨大白色瀑布从千仞高峰直泻而下，水雾弥漫升腾。阳光自画面左上方照射山峰，山间升腾起淡淡紫色烟雾，紫烟与云雾交融弥漫于峰顶。远景群山连绵淡墨渲染，近景松树点缀。瀑布如白练垂挂，水势磅礴，下方深潭水雾弥漫。
>
> 【接 STYLE】
>
> 横幅构图 16:9，山峰与瀑布位于画面中部偏上，画面右侧留出天空留白区域，画面下部留出水面与雾气空间以便放置文字。

### Prompt B：主插画备选（更写意）

> 中国水墨山水画，庐山瀑布意境。高耸的香炉峰，一道长长的白色瀑布从峰顶垂挂而下，如同银河坠落。山间紫烟缭绕，阳光穿云照射。远山淡墨，云雾弥漫，大面积极简留白。
>
> 【接 STYLE】
>
> 横幅构图 16:9，大量留白，构图简约空灵，画面左侧三分之二为山峰瀑布，右侧留白。

**实际选用**：B 版（留白更大、写意更足，给诗句留出了呼吸感）。**A 版也建议生成**，两版对比选优。

### Prompt C：作者画像（李白）

> 唐代诗人李白的中国画全身肖像。中年文人，头戴唐代幞头（软翅纱帽），身着白色宽袍大袖、腰束丝绦，手持折扇或酒盏，身姿飘逸洒脱，面庞清癯有神，长须微飘，目光望天，有仙风道骨之气。立姿全身像，居中构图，背景为纯色做旧宣纸底，无任何背景景物。中国工笔人物画风格，线条流畅，色彩淡雅，以墨色与淡彩为主。低饱和度，仿古宣纸米黄底色，画面有宣纸颗粒纹理。全身像，无文字、无印章。

---

## 二、页面生成 Prompt（第 ② 段 + 第 ③ 段）

**这一段就是你要丢给其他 agent 的主 prompt。** 直接整段复制：

---

### 📋 主 Prompt 全文

```
请生成一个单文件 HTML 页面（index.html），内容是李白的《望庐山瀑布》古诗展示页。

【技术约束】
- 单个 HTML 文件，CSS 和 JS 全部内联，不引用任何外部 CDN 或库
- 图片用 assets/main2.jpeg（主插画，水墨山水）和 assets/portrait.jpeg（李白画像）
- 所有资源用相对路径，不要用 /xxx 开头的绝对路径（要能部署在子目录）

【整体视觉】
- 仿古宣纸质感：米黄色底 + 明显的纸张颗粒噪点纹理
- 主插画作背景，用 mix-blend-mode: multiply 让它和纸色底融合
- 国风水墨审美，低饱和度，留白为主

【布局核心：竖排诗句】
- 四句诗，每句单独成一列，竖排（writing-mode: vertical-rl）
- 阅读顺序：从右向左
- 列间距均匀，字号要大（诗句是视觉主体）
- ⚠️ 坑：用 flex 容器做横排时，如果混用 direction: ltr 会导致元素尺寸算成 0 完全不可见。
     正确做法：横排容器用 `display:flex; flex-direction: row-reverse`（第一列落在最右），
     并给每列显式高度。不要依赖 direction: rtl。

【文字内容】
- 标题区（竖排，放在画面左侧）："唐 · 七言绝句" / "望庐山瀑布"（大字号）/ "李白"
- 诗句：
    日照香炉生紫烟
    遥看瀑布挂前川
    飞流直下三千尺
    疑是银河落九天
- 注释（小字，灰色）：香炉峰 · 庐山北峰，云雾缭绕如炉 ｜ 银河 · 银汉，即天河也
- 朱砂印章：红色方块 + 白字"诗仙之作"，竖排
- 作者卡：李白画像缩略图 + "李白 701—762 ｜ 字太白，号青莲居士" +
  简介（唐代伟大的浪漫主义诗人，被后世誉为「诗仙」。祖籍陇西成纪，生于碎叶城，
  五岁随父迁居绵州昌隆（今四川江油）。其诗想象奇绝、气势磅礴，善用夸张与神话，
  杜甫赞其「笔落惊风雨，诗成泣鬼神」。）+
  代表作（《静夜思》《将进酒》《望庐山瀑布》《早发白帝城》《蜀道难》）

【入场动画】
- 每列诗句从下往上逐列浮现（墨迹晕开感），用 CSS animation + animation-delay 做错峰
- 主插画做缓慢推拉（Ken Burns 效果）

【交互：物理引擎，这是重点】
写一个 PoemPhysics 类，实现"春风拂柳"质感：
- 每列诗是一个独立刚体，有 position / rotation / velocity
- 常驻低频正弦外力驱动，主周期 7.5s，模拟微风
- ⚠️ 关键：每列相位必须错开（实现里按 `(index/total) * π * 1.5` 均分，
  即 4 列相位各异）。这是"柳浪"错落感的核心 —— 同相位会像一整块板在晃动，很假
- 主波叠加一个更慢的次波（频率比例 0.43）和一次级相位，让摆动不重复
- 参数参考：横摆幅 ±5.2px，纵摆幅 ±2.6px，摆角 ±1.6°
- 拖动：按横向拖动速度让诗句弯曲倾斜（弯曲上限约 14°）
  - 源码参数：`BEND_MAX = 14`，即拖动越快倾斜越大，不会超过 14°
- 松手：惯性甩出 + 弹簧回拉 + 阻尼衰减，像柳枝一样荡回原位
- 用 requestAnimationFrame 循环，低通滤波防止抖动

【工具条】
底部浮动工具条（深色半透明），4 个按钮：
↻ 重播动画 ｜ 📖 讲解 ｜ ⬇ 导出图片 ｜ 👁 隐藏工具列 (H)
- "讲解"点开一个面板，包含 8 个知识点（创作背景、逐句赏析 ×4、艺术特色、拓展·李白写水的名句、朗读要点）
- "导出图片"用 html2canvas 思路自己实现（不引外部库）

【响应式】
- 移动端（< 768px）重排为竖版：标题横排居中 → 竖排诗句 → 注释 → 作者卡底部通栏
- 工具条在移动端收成 46×46 小圆钮，点开展开、点外部收起
- 矮屏（720p 笔记本）要注意纵向不能溢出：stage 宽高同时约束

【字体】
- 全站简体中文 + 宋体
- 字体栈：SimSun, 宋体, Songti SC, STSong, Noto Serif CJK SC, serif
- html lang="zh-CN"（⚠️ 标错成 zh-Hant 会导致简体字用繁体字形渲染）
```

---

## 三、交付后的验收清单

拿这份去检查对方的产出，一眼看出差距：

**基础项**
- [ ] 单文件、无外部依赖（断网也能开）
- [ ] 竖排是从右向左读的
- [ ] 四句诗是 4 列，不是排成 2×2 方格
- [ ] 资源路径全相对（部署到子目录不 404）

**交互项**
- [ ] 诗句能拖动，且拖动时有倾斜
- [ ] 松手有惯性回弹，不是硬切回原位
- [ ] 静置时四列都还在轻微摆动
- [ ] **四列摆动相位不同**（这条最容易翻车，同相位就像一块板在晃）

**细节项**
- [ ] 全站简体（用 `check_simplified.py` 扫，别靠肉眼）
- [ ] 字体是宋体/衬线，不是黑体
- [ ] 移动端不横向滚动
- [ ] 720p 笔记本不纵向溢出
- [ ] 印章、作者卡、注释互不重叠

---

## 四、最容易翻车的 5 个坑（提前告诉对方）

1. **flex + vertical-rl 元素尺寸算成 0** → 用 `flex-direction: row-reverse`，别用 `direction`
2. **诗句排成 2×2 方格** → 每列必须是一整个句子，不是逐字排
3. **`lang` 标成 zh-Hant** → 简体字被渲染成繁体字形，肉眼容易误判
4. **四列动画同相位** → 柳浪感全无，必须按 `(index/total) * π * 1.5` 错开相位
5. **Chrome headless 截图时 CSS 动画不推进** → `--virtual-time-budget` 会截出空画面，
   要用 Playwright 真实等待 4 秒再截

---

## 五、参考实现

- 代码：<https://github.com/youngtongyang/Chinese-Poem>
- 在线看：<https://youngtongyang.github.io/Chinese-Poem/>
- 出图脚本：`gen_images.py`（含完整 prompt 原文，可直接改诗换图）
- 简化字校验：`check_simplified.py`
- 物理 + 多视口体检：`test_physics.js`
